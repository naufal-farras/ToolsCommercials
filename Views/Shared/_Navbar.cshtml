@using ToolsCommercial.Models;
@using Microsoft.AspNetCore.Identity;
@using ToolsCommercial.Data;

@* @using ToolsCommercial.Models.UserManagement *@
@* @inject UserManager<User> _userManager *@
@inject AppDbContext _context

@{
    // // Access the UserLocation cookie
    // string userLocation = HttpContext.Request.Cookies["UserLocation"];

    // Use the userLocation as needed
    ViewBag.UserLocation = "Head Office"; // Default message if cookie is not found


}

<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-navy navbar-dark">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-action="Index" asp-controller="Home" class="nav-link">Home</a>
        </li>
    </ul>
    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
        <li class="nav-item">
            <a class="nav-link" data-widget="fullscreen" href="#" role="button">
                <i class="fas fa-expand-arrows-alt"></i>
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" data-widget="control-sidebar" data-controlsidebar-slide="true" href="#" role="button">
                <i class="fas fa-th-large"></i>
            </a>
        </li>
        <li class="nav-item dropdown">
            <a class="nav-link" data-toggle="dropdown" href="#">
                <i class="fas fa-user-circle"></i>
            </a>

            <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                <span class="dropdown-item dropdown-header font-weight-bold">Admin - @ViewBag.UserLocation</span>
                <div class="dropdown-divider"></div>
                <div class="dropdown-divider"></div>
                <a asp-action="Logout" asp-controller="Account" class="dropdown-item">
                    <i class="fas fa-sign-out-alt text-muted mr-1"></i>Log Out
                </a>
            </div>
        </li>
    </ul>
</nav>
<!-- /.navbar -->
